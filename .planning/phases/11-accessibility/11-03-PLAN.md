---
phase: 11-accessibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - MockPad/MockPad/Views/RequestDetailView.swift
  - MockPad/MockPad/Views/ResponseBodyEditorView.swift
  - MockPad/MockPad/Views/ResponseHeadersEditorView.swift
  - MockPad/MockPad/Views/ImportPreviewSheet.swift
  - MockPad/MockPad/Views/OpenAPIPreviewSheet.swift
  - MockPad/MockPad/Views/ProPaywallView.swift
  - MockPad/MockPad/Views/TemplatePickerView.swift
  - MockPad/MockPad/Views/EndpointEditorView.swift
  - MockPad/MockPad/Views/SettingsView.swift
  - MockPad/MockPad/Views/EmptyStateView.swift
  - MockPad/MockPad/Views/RequestLogListView.swift
autonomous: true

must_haves:
  truths:
    - "VoiceOver reads descriptive labels for the request detail summary row"
    - "VoiceOver reads validation badge state in response body editor"
    - "Response header remove buttons have VoiceOver label and meet 44pt minimum tap target"
    - "Import preview endpoint rows have composite VoiceOver labels"
    - "OpenAPI preview checkbox buttons have VoiceOver label and selection state"
    - "ProPaywallView decorative icons are hidden from VoiceOver"
    - "PRO lock overlays announce locked state and upgrade hint to VoiceOver"
    - "Ecosystem links in SettingsView have descriptive VoiceOver labels"
    - "Decorative empty state icons are hidden from VoiceOver"
    - "Reduce Motion environment property is declared for future animation guarding"
  artifacts:
    - path: "MockPad/MockPad/Views/RequestDetailView.swift"
      provides: "VoiceOver labels on summary section"
      contains: "accessibilityElement"
    - path: "MockPad/MockPad/Views/ResponseBodyEditorView.swift"
      provides: "VoiceOver label on validation badge"
      contains: "accessibilityLabel"
    - path: "MockPad/MockPad/Views/ResponseHeadersEditorView.swift"
      provides: "VoiceOver label and 44pt target on remove button"
      contains: "accessibilityLabel"
    - path: "MockPad/MockPad/Views/ImportPreviewSheet.swift"
      provides: "VoiceOver composite labels on endpoint rows"
      contains: "accessibilityElement"
    - path: "MockPad/MockPad/Views/OpenAPIPreviewSheet.swift"
      provides: "VoiceOver labels and selection state on checkboxes"
      contains: "accessibilityLabel"
    - path: "MockPad/MockPad/Views/ProPaywallView.swift"
      provides: "Decorative icons hidden from VoiceOver"
      contains: "accessibilityHidden"
    - path: "MockPad/MockPad/Views/EndpointEditorView.swift"
      provides: "PRO lock overlay accessibility"
      contains: "accessibilityLabel"
    - path: "MockPad/MockPad/Views/SettingsView.swift"
      provides: "Ecosystem link accessibility labels"
      contains: "accessibilityLabel"
  key_links:
    - from: "MockPad/MockPad/Views/EndpointEditorView.swift"
      to: "VoiceOver"
      via: "PRO overlay accessibility label"
      pattern: "accessibilityLabel.*PRO"
---

<objective>
Add VoiceOver labels, decorative element hiding, PRO lock overlay accessibility, and Reduce Motion convention to the remaining 11 view files. This completes ACCS-01 (VoiceOver), ACCS-03 (Reduce Motion), and remaining ACCS-05 (tap targets) coverage across the entire app.

Purpose: Complete accessibility coverage for all secondary views -- detail screens, editors, sheets, paywall, settings, and import previews. Also establish the Reduce Motion pattern for future animation additions.
Output: 11 view files updated with accessibility modifiers, completing full-app VoiceOver coverage.
</objective>

<execution_context>
@/home/claudeuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-accessibility/11-RESEARCH.md
@MockPad/MockPad/Views/RequestDetailView.swift
@MockPad/MockPad/Views/ResponseBodyEditorView.swift
@MockPad/MockPad/Views/ResponseHeadersEditorView.swift
@MockPad/MockPad/Views/ImportPreviewSheet.swift
@MockPad/MockPad/Views/OpenAPIPreviewSheet.swift
@MockPad/MockPad/Views/ProPaywallView.swift
@MockPad/MockPad/Views/TemplatePickerView.swift
@MockPad/MockPad/Views/EndpointEditorView.swift
@MockPad/MockPad/Views/SettingsView.swift
@MockPad/MockPad/Views/EmptyStateView.swift
@MockPad/MockPad/Views/RequestLogListView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: VoiceOver for detail views, editors, and import sheets</name>
  <files>MockPad/MockPad/Views/RequestDetailView.swift, MockPad/MockPad/Views/ResponseBodyEditorView.swift, MockPad/MockPad/Views/ResponseHeadersEditorView.swift, MockPad/MockPad/Views/ImportPreviewSheet.swift, MockPad/MockPad/Views/OpenAPIPreviewSheet.swift</files>
  <action>
**RequestDetailView.swift:**
In `requestSummarySection`, add `.accessibilityElement(children: .combine)` to the outer VStack so VoiceOver reads method, path, status code, timestamp, and response time as one unit.

Add `.accessibilityLabel("Copy request as cURL command")` to the `copyAsCurlButton` Button. When `showCopiedFeedback` is true, the label already changes to "Copied!" via the Label text -- the existing text is sufficient, but add `.accessibilityLabel(showCopiedFeedback ? "Copied to clipboard" : "Copy as cURL command")` for clearer VoiceOver announcement.

**ResponseBodyEditorView.swift:**
Add to the `validationBadge` `.valid` HStack:
```swift
.accessibilityElement(children: .combine)
.accessibilityLabel("JSON validation: valid")
```

Add to the `validationBadge` `.invalid` HStack:
```swift
.accessibilityElement(children: .combine)
.accessibilityLabel("JSON validation: invalid")
```

**ResponseHeadersEditorView.swift:**
Add to the remove button (the `minus.circle.fill` Button) after `.buttonStyle(.plain)`:
```swift
.accessibilityLabel("Remove header")
.frame(minWidth: 44, minHeight: 44)
.contentShape(Rectangle())
```

Add `.accessibilityElement(children: .combine)` to each header row HStack (the one containing Key TextField, Value TextField, and remove button) -- actually, do NOT combine the HStack since TextFields need individual focus. Instead, only add the label and frame to the remove button.

**ImportPreviewSheet.swift:**
Add `.accessibilityElement(children: .combine)` and `.accessibilityLabel("\(endpoint.httpMethod) \(endpoint.path), status \(endpoint.responseStatusCode)")` to each endpoint HStack in the ForEach.

**OpenAPIPreviewSheet.swift:**
Add to each checkbox Button (the one with `checkmark.square.fill` / `square`) after `.buttonStyle(.plain)`:
```swift
.accessibilityLabel(selections[index] ? "Deselect \(endpoint.httpMethod) \(endpoint.path)" : "Select \(endpoint.httpMethod) \(endpoint.path)")
```

Add `.accessibilityElement(children: .combine)` to each endpoint's outer VStack and add a descriptive label:
```swift
.accessibilityLabel("\(endpoint.httpMethod) \(endpoint.path), status \(endpoint.responseStatusCode)\(endpoint.summary.map { ", \($0)" } ?? ""), \(selections[index] ? "selected" : "not selected")")
```
Actually, since the VStack contains a button, use `.contain` instead of `.combine` to keep the checkbox button interactive.
  </action>
  <verify>Build the project to confirm no compilation errors. Grep for `accessibilityLabel\|accessibilityElement` in all 5 files.</verify>
  <done>All detail views, editors, and import sheets have descriptive VoiceOver labels. Header remove buttons have 44pt tap targets.</done>
</task>

<task type="auto">
  <name>Task 2: VoiceOver for ProPaywallView, PRO overlays, SettingsView, empty states, and Reduce Motion convention</name>
  <files>MockPad/MockPad/Views/ProPaywallView.swift, MockPad/MockPad/Views/EndpointEditorView.swift, MockPad/MockPad/Views/TemplatePickerView.swift, MockPad/MockPad/Views/SettingsView.swift, MockPad/MockPad/Views/EmptyStateView.swift, MockPad/MockPad/Views/RequestLogListView.swift</files>
  <action>
**ProPaywallView.swift:**
Hide decorative icons from VoiceOver:
- Add `.accessibilityHidden(true)` to the `lock.shield.fill` Image
- Add `.accessibilityHidden(true)` to each `checkmark` Image in the feature list
- Add `.accessibilityHidden(true)` to each feature icon Image (the `features[index].icon` Image)
- Add `.accessibilityElement(children: .combine)` to each feature HStack row so VoiceOver reads "Unlimited Endpoints, included" as one element
- Add `.accessibilityHidden(true)` to the dismiss `xmark` button's Image (the button itself is fine, label comes from the Image -- add `.accessibilityLabel("Dismiss")` to the Button instead)

**EndpointEditorView.swift:**
For each PRO overlay (Collection section, Response Delay section), add to the `Color.clear` overlay:
```swift
.accessibilityLabel("PRO feature, locked")
.accessibilityHint("Double tap to view PRO upgrade")
```

There are 3 PRO overlays in EndpointEditorView (Collection section, Response Delay section, and the one in TemplatePickerView). Handle the first two here.

Add `.accessibilityLabel("Response delay, \(endpoint.responseDelayMs) milliseconds")` and `.accessibilityValue("\(endpoint.responseDelayMs) milliseconds")` to the Slider.

**TemplatePickerView.swift:**
Add `.accessibilityLabel("PRO feature, locked")` and `.accessibilityHint("Double tap to view PRO upgrade")` to the PRO lock button in the non-PRO else branch.

**SettingsView.swift:**
Add `.accessibilityLabel("\(name), \(subtitle), opens App Store")` to the ecosystem link view returned by the `ecosystemLink` function. Apply it to the Link element.

Add `.accessibilityLabel("Clear request log")` to the trash button in the DATA section (it already has a Label, but ensure it reads clearly).

**EmptyStateView.swift:**
Add `.accessibilityHidden(true)` to the decorative `antenna.radiowaves.left.and.right.slash` Image since the "No Endpoints" text below provides the meaning.

**RequestLogListView.swift:**
In `emptyStateView`, add `.accessibilityHidden(true)` to all three decorative SF Symbol Images (`server.rack`, `antenna.radiowaves.left.and.right`, `line.3.horizontal.decrease.circle`) since the text below each provides the meaning.

Add `.accessibilityLabel("Clear request log")` to the trash toolbar button.

**Reduce Motion convention (ACCS-03):**
The codebase currently has zero animations. Add a `@Environment(\.accessibilityReduceMotion) private var reduceMotion` declaration to ServerStatusBarView.swift (the most likely place for future status dot pulse animation). This establishes the pattern without changing behavior.

Note: ServerStatusBarView is modified in Plan 02, but this environment property addition is a single-line property declaration that doesn't conflict with Plan 02's accessibility modifier additions. If there's a merge concern, the executor should add it alongside the other changes. Actually, to avoid file conflicts, add the `@Environment(\.accessibilityReduceMotion)` property to **EmptyStateView.swift** instead (since this plan already modifies it), with a comment: `// Reduce Motion: guard any future animations with this property (ACCS-03)`.
  </action>
  <verify>Build the project to confirm no compilation errors. Grep for `accessibilityHidden\|accessibilityLabel\|accessibilityHint\|reduceMotion` across all 6 files to confirm modifiers were added.</verify>
  <done>ProPaywallView decorative icons hidden from VoiceOver. PRO lock overlays announce locked state. SettingsView ecosystem links have descriptive labels. Empty state icons hidden from VoiceOver. Reduce Motion environment property established for future use.</done>
</task>

</tasks>

<verification>
1. `grep -rn "accessibilityLabel\|accessibilityHidden\|accessibilityHint\|accessibilityElement\|reduceMotion" MockPad/MockPad/Views/RequestDetailView.swift MockPad/MockPad/Views/ResponseBodyEditorView.swift MockPad/MockPad/Views/ResponseHeadersEditorView.swift MockPad/MockPad/Views/ImportPreviewSheet.swift MockPad/MockPad/Views/OpenAPIPreviewSheet.swift MockPad/MockPad/Views/ProPaywallView.swift MockPad/MockPad/Views/EndpointEditorView.swift MockPad/MockPad/Views/TemplatePickerView.swift MockPad/MockPad/Views/SettingsView.swift MockPad/MockPad/Views/EmptyStateView.swift MockPad/MockPad/Views/RequestLogListView.swift` shows accessibility modifiers in all 11 files
2. `grep -r "reduceMotion" MockPad/MockPad/Views/` shows Reduce Motion environment property
3. Project builds without errors
</verification>

<success_criteria>
- All remaining views have VoiceOver labels on interactive and informational elements
- Decorative icons hidden from VoiceOver (ProPaywallView header, empty state icons, feature checkmarks)
- PRO lock overlays announce "PRO feature, locked" with upgrade hint
- Response header remove buttons meet 44pt minimum tap target
- Reduce Motion environment property established for future animation guarding
- Combined with Plans 01 and 02, all 22 views have accessibility coverage
</success_criteria>

<output>
After completion, create `.planning/phases/11-accessibility/11-03-SUMMARY.md`
</output>
