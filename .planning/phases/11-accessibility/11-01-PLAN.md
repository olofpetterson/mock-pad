---
phase: 11-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MockPad/MockPad/Theme/MockPadColors.swift
  - MockPad/MockPad/Views/EmptyStateView.swift
  - MockPad/MockPad/Views/RequestLogListView.swift
  - MockPad/MockPad/Views/ProPaywallView.swift
autonomous: true

must_haves:
  truths:
    - "PATCH and DELETE method badges have visually distinct luminance values (luminance gap >= 0.06)"
    - "All five HTTP method colors have distinct luminance bands separated by at least 0.06"
    - "EmptyStateView icon scales with Dynamic Type instead of fixed 48pt"
    - "RequestLogListView empty state icons scale with Dynamic Type instead of fixed 40pt"
    - "ProPaywallView header icon scales with Dynamic Type instead of fixed 42pt"
  artifacts:
    - path: "MockPad/MockPad/Theme/MockPadColors.swift"
      provides: "Adjusted method color with distinct luminance"
      contains: "methodDelete"
    - path: "MockPad/MockPad/Views/EmptyStateView.swift"
      provides: "ScaledMetric icon sizing"
      contains: "@ScaledMetric"
    - path: "MockPad/MockPad/Views/RequestLogListView.swift"
      provides: "ScaledMetric icon sizing"
      contains: "@ScaledMetric"
    - path: "MockPad/MockPad/Views/ProPaywallView.swift"
      provides: "ScaledMetric icon sizing"
      contains: "@ScaledMetric"
  key_links:
    - from: "MockPad/MockPad/Theme/MockPadColors.swift"
      to: "All views using methodColor(for:)"
      via: "static color lookup"
      pattern: "methodDelete"
---

<objective>
Fix theme-level accessibility foundations: adjust HTTP method badge colors for color blindness luminance differentiation (ACCS-04) and replace all fixed-size font declarations with @ScaledMetric for Dynamic Type compliance (ACCS-02).

Purpose: These are foundational changes that affect the entire app -- color tokens used by every method badge, and fixed font sizes in 3 views that break Dynamic Type scaling.
Output: Updated MockPadColors.swift with distinct luminance method colors, and 3 views with @ScaledMetric replacing .font(.system(size: N)).
</objective>

<execution_context>
@/home/claudeuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@MockPad/MockPad/Theme/MockPadColors.swift
@MockPad/MockPad/Views/EmptyStateView.swift
@MockPad/MockPad/Views/RequestLogListView.swift
@MockPad/MockPad/Views/ProPaywallView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adjust HTTP method badge colors for distinct luminance (ACCS-04)</name>
  <files>MockPad/MockPad/Theme/MockPadColors.swift</files>
  <action>
Adjust the `methodDelete` color in MockPadColors.swift to have distinct luminance from `methodPatch`. Currently both PATCH (#7B68EE, luminance ~0.22) and DELETE (#FF4D4D, luminance ~0.22) have nearly identical luminance, making them indistinguishable for color-blind users.

Change `methodDelete` from `Color(red: 1.0, green: 0.30, blue: 0.30)` (#FF4D4D) to `Color(red: 1.0, green: 0.42, blue: 0.42)` (#FF6B6B) which has luminance ~0.28, creating a clear separation.

Also update `serverStopped` to match the new DELETE red for visual consistency: change from `Color(red: 1.0, green: 0.30, blue: 0.30)` to `Color(red: 1.0, green: 0.42, blue: 0.42)`.

Also update `status5xx` to match: change from `Color(red: 1.0, green: 0.30, blue: 0.30)` to `Color(red: 1.0, green: 0.42, blue: 0.42)`.

This ensures the five method color luminance bands are:
1. GET: ~0.71 (brightest)
2. POST: ~0.55
3. PUT: ~0.49
4. DELETE: ~0.28 (adjusted up)
5. PATCH: ~0.22 (darkest)

Update the inline comments to reflect the new hex values.
  </action>
  <verify>Build the project to confirm no compilation errors. Visually verify the color constant values match the specified RGB.</verify>
  <done>methodDelete, serverStopped, and status5xx use the lighter red (#FF6B6B) with luminance ~0.28, clearly distinct from PATCH luminance ~0.22.</done>
</task>

<task type="auto">
  <name>Task 2: Replace fixed-size fonts with @ScaledMetric for Dynamic Type (ACCS-02)</name>
  <files>MockPad/MockPad/Views/EmptyStateView.swift, MockPad/MockPad/Views/RequestLogListView.swift, MockPad/MockPad/Views/ProPaywallView.swift</files>
  <action>
Replace all 5 occurrences of `.font(.system(size: N))` with `@ScaledMetric` property wrappers so icons scale with Dynamic Type.

**EmptyStateView.swift** (1 occurrence):
- Add property: `@ScaledMetric(relativeTo: .largeTitle) private var emptyIconSize: CGFloat = 48`
- Change `.font(.system(size: 48))` to `.font(.system(size: emptyIconSize))`

**RequestLogListView.swift** (3 occurrences in emptyStateView):
- Add property: `@ScaledMetric(relativeTo: .title) private var emptyIconSize: CGFloat = 40`
- Change all three `.font(.system(size: 40))` to `.font(.system(size: emptyIconSize))`

**ProPaywallView.swift** (1 occurrence):
- Add property: `@ScaledMetric(relativeTo: .largeTitle) private var headerIconSize: CGFloat = 42`
- Change `.font(.system(size: 42))` to `.font(.system(size: headerIconSize))`

Place each `@ScaledMetric` property declaration alongside the other property declarations at the top of each struct (after @Environment declarations, before `var body`).
  </action>
  <verify>Build the project to confirm no compilation errors. Grep for `.font(.system(size:` across all view files to confirm zero remaining fixed-size font occurrences.</verify>
  <done>All 5 fixed-size font declarations replaced with @ScaledMetric. Zero occurrences of `.font(.system(size: N))` remain in view files.</done>
</task>

</tasks>

<verification>
1. `grep -r "\.font(\.system(size:" MockPad/MockPad/Views/` returns zero results
2. MockPadColors.swift contains `methodDelete = Color(red: 1.0, green: 0.42, blue: 0.42)`
3. Project builds without errors
</verification>

<success_criteria>
- HTTP method colors have 5 distinct luminance bands with minimum 0.06 separation between adjacent bands
- All decorative icons in EmptyStateView, RequestLogListView, and ProPaywallView scale with Dynamic Type
- Zero `.font(.system(size: N))` calls remain in view files
</success_criteria>

<output>
After completion, create `.planning/phases/11-accessibility/11-01-SUMMARY.md`
</output>
