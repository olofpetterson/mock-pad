---
phase: 07-import-export-collections
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - MockPad/MockPad/Models/MockEndpoint.swift
  - MockPad/MockPad/App/EndpointStore.swift
  - MockPad/MockPad/Services/CollectionExporter.swift
  - MockPad/MockPad/Services/CollectionImporter.swift
  - MockPad/MockPad/Services/MockPadExportModels.swift
  - MockPad/MockPad/Services/EndpointSnapshot.swift
  - MockPad/MockPadTests/CollectionExporterTests.swift
  - MockPad/MockPadTests/CollectionImporterTests.swift
autonomous: true

must_haves:
  truths:
    - "MockEndpoint has optional collectionName property for collection grouping"
    - "EndpointStore can derive sorted list of distinct collection names"
    - "CollectionExporter produces valid versioned JSON with format identifier"
    - "CollectionImporter parses MockPad JSON and rejects invalid/unknown formats"
    - "Duplicate detection identifies matching endpoints by path+method (case-insensitive)"
  artifacts:
    - path: "MockPad/MockPad/Models/MockEndpoint.swift"
      provides: "collectionName: String? property"
      contains: "collectionName"
    - path: "MockPad/MockPad/Services/CollectionExporter.swift"
      provides: "JSON export from endpoint data"
      exports: ["CollectionExporter"]
    - path: "MockPad/MockPad/Services/CollectionImporter.swift"
      provides: "JSON import with validation and duplicate detection"
      exports: ["CollectionImporter", "DuplicateResolution"]
    - path: "MockPad/MockPad/Services/MockPadExportModels.swift"
      provides: "Codable export format structs"
      exports: ["MockPadExport", "ExportedEndpoint", "MockPadDocument", "MockPadExportFile"]
  key_links:
    - from: "MockPad/MockPad/Services/CollectionExporter.swift"
      to: "MockPad/MockPad/Services/MockPadExportModels.swift"
      via: "Encodes MockPadExport to JSON Data"
      pattern: "JSONEncoder.*encode.*MockPadExport"
    - from: "MockPad/MockPad/Services/CollectionImporter.swift"
      to: "MockPad/MockPad/Services/MockPadExportModels.swift"
      via: "Decodes JSON Data to MockPadExport"
      pattern: "JSONDecoder.*decode.*MockPadExport"
    - from: "MockPad/MockPad/App/EndpointStore.swift"
      to: "MockPad/MockPad/Models/MockEndpoint.swift"
      via: "Queries distinct collectionName values"
      pattern: "collectionName"
---

<objective>
Data layer and services for endpoint collections, JSON export/import, and duplicate detection.

Purpose: Establish the model property (collectionName), Codable export format (MockPadExport), export service (CollectionExporter), import service (CollectionImporter) with validation and duplicate detection. All pure-function services amenable to TDD.

Output: MockEndpoint gains collectionName, EndpointStore gains collection helpers, two caseless enum services with full test coverage, Codable format structs, MockPadDocument for fileExporter, MockPadExportFile Transferable for ShareLink.
</objective>

<execution_context>
@/home/claudeuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@MockPad/MockPad/Models/MockEndpoint.swift
@MockPad/MockPad/App/EndpointStore.swift
@MockPad/MockPad/Services/EndpointSnapshot.swift
@MockPad/MockPad/Services/BuiltInTemplates.swift
@MockPad/MockPad/App/ProManager.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model + Codable structs + export/import services with RED tests</name>
  <files>
    MockPad/MockPad/Models/MockEndpoint.swift
    MockPad/MockPad/App/EndpointStore.swift
    MockPad/MockPad/Services/EndpointSnapshot.swift
    MockPad/MockPad/Services/MockPadExportModels.swift
    MockPad/MockPad/Services/CollectionExporter.swift
    MockPad/MockPad/Services/CollectionImporter.swift
    MockPad/MockPadTests/CollectionExporterTests.swift
    MockPad/MockPadTests/CollectionImporterTests.swift
  </files>
  <action>
    **MockEndpoint.swift** — Add `var collectionName: String?` property (defaults to nil). Add `collectionName: String? = nil` parameter to init, assign `self.collectionName = collectionName`.

    **EndpointStore.swift** — Add computed property `collectionNames: [String]` that returns `Set(endpoints.compactMap(\.collectionName)).sorted()`. Add method `func endpoints(inCollection name: String?) -> [MockEndpoint]` using FetchDescriptor with `#Predicate { $0.collectionName == name }` sorted by sortOrder. Add method `func importEndpoints(_ endpoints: [ExportedEndpoint], collectionName: String?, resolution: DuplicateResolution)` that creates MockEndpoint from each ExportedEndpoint and inserts via modelContext. For `.skip` resolution, filter out duplicates before inserting. For `.replace`, delete existing matching endpoint first. For `.importAsNew`, insert without checking. After all inserts, `try? modelContext.save()`.

    **EndpointSnapshot.swift** — No change needed (EndpointSnapshot is for server engine, not export).

    **MockPadExportModels.swift** (NEW) — Create file with:
    - `struct MockPadExport: Codable` with fields: `format: String`, `version: Int`, `exportedAt: Date`, `collectionName: String?`, `endpoints: [ExportedEndpoint]`
    - `struct ExportedEndpoint: Codable` with fields: `path: String`, `httpMethod: String`, `responseStatusCode: Int`, `responseBody: String`, `responseHeaders: [String: String]`, `isEnabled: Bool`, `responseDelayMs: Int`
    - `enum DuplicateResolution: String, CaseIterable` with cases: `skip`, `replace`, `importAsNew`
    - `struct MockPadDocument: FileDocument` — `static var readableContentTypes: [UTType] { [.json] }`. Init from `MockPadExport` using JSONEncoder with `.prettyPrinted`, `.sortedKeys`, `.iso8601` date strategy. Init from `ReadConfiguration` reading `regularFileContents`. `fileWrapper` returns `FileWrapper(regularFileWithContents: data)`. Need `import UniformTypeIdentifiers`.
    - `struct MockPadExportFile: Transferable` with `data: Data`, `filename: String`. `static var transferRepresentation: some TransferRepresentation` using `FileRepresentation(exportedContentType: .json)` that writes data to temp file at `FileManager.default.temporaryDirectory.appendingPathComponent(exportFile.filename + ".json")` and returns `SentTransferredFile(tempURL)`.

    **CollectionExporter.swift** (NEW) — Caseless enum `CollectionExporter` (matches BuiltInTemplates pattern) with:
    - `static func export(endpoints: [MockEndpoint], collectionName: String?) throws -> Data` — Maps MockEndpoint array to ExportedEndpoint array, creates MockPadExport with `format: "mockpad-collection"`, `version: 1`, `exportedAt: Date()`. Encodes with JSONEncoder using `.prettyPrinted`, `.sortedKeys`, `.iso8601`.
    - `static func exportDocument(endpoints: [MockEndpoint], collectionName: String?) throws -> MockPadDocument` — Calls export() then wraps in MockPadDocument.

    **CollectionImporter.swift** (NEW) — Caseless enum `CollectionImporter` with:
    - `enum ImportError: Error, LocalizedError` with cases: `.invalidFormat`, `.unsupportedVersion(Int)`, `.decodingFailed(String)`. Each has `errorDescription`.
    - `static func parse(data: Data) throws -> MockPadExport` — JSONDecoder with `.iso8601`, decode MockPadExport, verify `format == "mockpad-collection"`, verify `version == 1`.
    - `static func findDuplicates(imported: [ExportedEndpoint], existing: [MockEndpoint]) -> [ExportedEndpoint]` — Filter imported where existing contains matching `path.lowercased() == imp.path.lowercased() && httpMethod.uppercased() == imp.httpMethod.uppercased()`.

    **Tests (RED):**

    **CollectionExporterTests.swift** — `import Testing`, `@testable import MockPad`, `@MainActor struct CollectionExporterTests`:
    1. `test_export_producesValidJSON` — Create two MockEndpoint (in-memory context), export, decode result, verify format == "mockpad-collection", version == 1, endpoints.count == 2
    2. `test_export_includesAllEndpointFields` — Export one endpoint with all fields set (custom headers, delay, status 201), verify ExportedEndpoint fields match
    3. `test_export_withCollectionName` — Export with collectionName "My API", verify in decoded result
    4. `test_export_withNilCollectionName` — Export with nil collectionName, verify null in JSON
    5. `test_export_sortedKeys` — Export, convert Data to String, verify keys appear in alphabetical order

    **CollectionImporterTests.swift** — `import Testing`, `@testable import MockPad`, `struct CollectionImporterTests`:
    1. `test_parse_validExport` — Create valid MockPadExport JSON, parse, verify fields
    2. `test_parse_invalidFormat` — JSON with format "not-mockpad", expect ImportError.invalidFormat
    3. `test_parse_unsupportedVersion` — JSON with version 99, expect ImportError.unsupportedVersion
    4. `test_parse_invalidJSON` — Random string data, expect ImportError.decodingFailed
    5. `test_findDuplicates_matchByPathAndMethod` — 2 imported + 1 matching existing, verify 1 duplicate found
    6. `test_findDuplicates_caseInsensitive` — "/API/Users" matches "/api/users", "get" matches "GET"
    7. `test_findDuplicates_differentMethodNotDuplicate` — Same path different method, verify 0 duplicates
    8. `test_findDuplicates_noDuplicates` — All unique, verify empty result

    Run tests — all should FAIL (RED) since services are stubs.
  </action>
  <verify>Tests compile. Tests fail because service implementations are stubs or not yet complete. `swift build` succeeds for main target.</verify>
  <done>MockEndpoint has collectionName, EndpointStore has collection helpers, all Codable structs exist, service files exist with method signatures, MockPadDocument and MockPadExportFile exist, 13 tests written and failing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement services to pass all tests (GREEN) + refactor</name>
  <files>
    MockPad/MockPad/Services/CollectionExporter.swift
    MockPad/MockPad/Services/CollectionImporter.swift
    MockPad/MockPad/Services/MockPadExportModels.swift
    MockPad/MockPad/App/EndpointStore.swift
    MockPad/MockPadTests/CollectionExporterTests.swift
    MockPad/MockPadTests/CollectionImporterTests.swift
  </files>
  <action>
    Implement all service methods fully so all 13 tests pass:

    **CollectionExporter** — Complete the export() method: map MockEndpoint properties to ExportedEndpoint (path, httpMethod, responseStatusCode, responseBody, responseHeaders, isEnabled, responseDelayMs). Create MockPadExport with format "mockpad-collection", version 1, Date(), collectionName, mapped endpoints. Encode with JSONEncoder (prettyPrinted, sortedKeys, iso8601).

    **CollectionImporter** — Complete parse(): decode with iso8601 date strategy, guard format and version. Complete findDuplicates(): filter with lowercased path + uppercased method comparison.

    **EndpointStore.importEndpoints()** — Implement the three resolution branches. For `.skip`: filter out ExportedEndpoints that have matching path+method in existing endpoints. For `.replace`: for each imported endpoint, delete any existing with same path+method, then insert new. For `.importAsNew`: insert all without checking. Each imported ExportedEndpoint becomes a MockEndpoint with sortOrder = current max + 1 + index. Assign collectionName from the import's collectionName. Save context.

    Run tests — all 13 should PASS (GREEN).

    **REFACTOR** — Review for any cleanup. Ensure JSONEncoder configuration is consistent (same settings in CollectionExporter and MockPadDocument). Extract shared encoder config if duplicated.
  </action>
  <verify>Run `swift test` — all 13 new tests pass plus all existing tests still pass. No regressions.</verify>
  <done>CollectionExporter produces valid versioned JSON. CollectionImporter parses and validates format. Duplicate detection works with case-insensitive path+method matching. EndpointStore can import endpoints with skip/replace/importAsNew resolution. All tests green.</done>
</task>

</tasks>

<verification>
1. `swift test` — all tests pass (existing + 13 new)
2. MockEndpoint.collectionName property exists and accepts nil default
3. CollectionExporter.export() returns Data that decodes to MockPadExport with format "mockpad-collection" and version 1
4. CollectionImporter.parse() rejects non-MockPad JSON with descriptive errors
5. CollectionImporter.findDuplicates() matches by path+method case-insensitively
6. MockPadDocument conforms to FileDocument with JSON content type
7. MockPadExportFile conforms to Transferable with FileRepresentation
</verification>

<success_criteria>
All 13 tests pass. MockEndpoint has collectionName. Export produces valid JSON. Import validates format and detects duplicates. MockPadDocument and MockPadExportFile ready for UI integration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-import-export-collections/07-01-SUMMARY.md`
</output>
