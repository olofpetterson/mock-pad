---
phase: 07-import-export-collections
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - MockPad/MockPad/Views/EndpointListView.swift
  - MockPad/MockPad/Views/EndpointEditorView.swift
  - MockPad/MockPad/Views/CollectionFilterChipsView.swift
autonomous: true

must_haves:
  truths:
    - "User can filter endpoint list by collection name using filter chips"
    - "User can assign an endpoint to a collection from the editor (PRO)"
    - "User can create a new collection name from the editor (PRO)"
    - "User sees all endpoints when no collection filter is active"
  artifacts:
    - path: "MockPad/MockPad/Views/CollectionFilterChipsView.swift"
      provides: "Horizontal scrollable filter chips for collection filtering"
      contains: "CollectionFilterChipsView"
    - path: "MockPad/MockPad/Views/EndpointListView.swift"
      provides: "Collection filter state and filtered endpoint display"
      contains: "selectedCollection"
    - path: "MockPad/MockPad/Views/EndpointEditorView.swift"
      provides: "Collection assignment picker section"
      contains: "collectionName"
  key_links:
    - from: "MockPad/MockPad/Views/CollectionFilterChipsView.swift"
      to: "MockPad/MockPad/App/EndpointStore.swift"
      via: "Reads collectionNames from EndpointStore"
      pattern: "endpointStore\\.collectionNames"
    - from: "MockPad/MockPad/Views/EndpointEditorView.swift"
      to: "MockPad/MockPad/Models/MockEndpoint.swift"
      via: "Binds to endpoint.collectionName"
      pattern: "endpoint\\.collectionName"
---

<objective>
Collection management UI: filter chips for endpoint list filtering and collection assignment in the endpoint editor.

Purpose: Let PRO users organize endpoints into named collections and filter the endpoint list by collection. The filter chips match the existing LogFilterChipsView pattern for UI consistency.

Output: CollectionFilterChipsView, EndpointListView with collection filtering, EndpointEditorView with collection assignment section.
</objective>

<execution_context>
@/home/claudeuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-import-export-collections/07-01-SUMMARY.md
@MockPad/MockPad/Views/EndpointListView.swift
@MockPad/MockPad/Views/EndpointEditorView.swift
@MockPad/MockPad/Views/LogFilterChipsView.swift
@MockPad/MockPad/App/EndpointStore.swift
@MockPad/MockPad/App/ProManager.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: CollectionFilterChipsView + EndpointListView collection filtering</name>
  <files>
    MockPad/MockPad/Views/CollectionFilterChipsView.swift
    MockPad/MockPad/Views/EndpointListView.swift
  </files>
  <action>
    **CollectionFilterChipsView.swift** (NEW) — Create a horizontal ScrollView with filter chips matching the LogFilterChipsView visual pattern. Takes `@Binding var selectedCollection: String?` and reads `@Environment(EndpointStore.self) private var endpointStore`. Displays:
    - "All" chip — always shown, active when selectedCollection is nil. Tapping sets selectedCollection = nil.
    - One chip per collection name from `endpointStore.collectionNames` — active when selectedCollection matches. Tapping toggles: if already selected, set nil (show all); otherwise set to this name.
    - Chip styling: use same pattern as LogFilterChipsView (MockPadTypography.badge font, MockPadColors.accent for active chip background, MockPadColors.panel2 for inactive, MockPadColors.border stroke, cornerRadius MockPadMetrics.cornerRadiusSmall).
    - Only show the entire view when `endpointStore.collectionNames` is not empty (no filter bar if no collections exist).
    - PRO gate the entire view: wrap in `Group { }.opacity(proManager.isPro ? 1 : 0.4).allowsHitTesting(proManager.isPro)`. Read `@Environment(ProManager.self) private var proManager`.

    **EndpointListView.swift** — Add `@State private var selectedCollection: String?` (nil = show all). Insert `CollectionFilterChipsView(selectedCollection: $selectedCollection)` above the List (between the .scrollContentBackground and .navigationTitle, OR inside the List as a Section at the top — prefer above the List to avoid interfering with ForEach move/delete).

    For filtering: replace `endpointStore.endpoints` in the ForEach with a computed property `filteredEndpoints` that returns `selectedCollection == nil ? endpointStore.endpoints : endpointStore.endpoints.filter { $0.collectionName == selectedCollection }`.

    Also update `duplicateEndpoint` to copy `collectionName` from source: add `collectionName: source.collectionName` parameter when creating the copy.
  </action>
  <verify>Build succeeds. The endpoint list shows collection filter chips when collections exist. Tapping "All" shows all endpoints. Tapping a collection name filters to that collection. Filter chips hidden when no collections exist. Free users see dimmed, non-interactive chips.</verify>
  <done>Collection filter chips appear above endpoint list when collections exist. Filtering works by selected collection. "All" chip resets filter. PRO-gated with opacity/allowsHitTesting pattern. Duplicate preserves collectionName.</done>
</task>

<task type="auto">
  <name>Task 2: Collection assignment in EndpointEditorView</name>
  <files>
    MockPad/MockPad/Views/EndpointEditorView.swift
  </files>
  <action>
    **EndpointEditorView.swift** — Add a new Form Section for collection assignment, placed after the STATUS CODE section and before the template picker section. Blueprint label: `"> COLLECTION_"`.

    Inside the section, create a PRO-gated Group (opacity/allowsHitTesting pattern) containing:

    1. **Picker for existing collections** — `Picker("Collection", selection: $endpoint.collectionName)` with:
       - `Text("None").tag(String?.none)` for no collection
       - `ForEach(endpointStore.collectionNames, id: \.self) { name in Text(name).tag(String?.some(name)) }` for existing collections
       - If there's a `newCollectionName` value, also show it as an option

    2. **"New Collection" button** — A `Button` that toggles `@State private var showNewCollectionField = false`. When showNewCollectionField is true, show a `TextField("Collection name", text: $newCollectionName)` with a "Create" button. On Create: set `endpoint.collectionName = newCollectionName`, reset showNewCollectionField to false, clear newCollectionName, call `saveAndSync()`.

    Add `@State private var newCollectionName = ""` and `@State private var showNewCollectionField = false` state variables.

    The Picker should use `.pickerStyle(.menu)` for compact display.

    Add `.onChange(of: endpoint.collectionName)` to call `saveAndSync()` (same pattern as other onChange handlers in this view).

    Section background: `.listRowBackground(MockPadColors.panel)`.
  </action>
  <verify>Build succeeds. Editor shows COLLECTION section with picker. "None" option removes collection assignment. Existing collection names appear in picker. "New Collection" flow creates a new collection name and assigns endpoint. PRO-gated (dimmed + non-interactive for free users). Changes auto-save.</verify>
  <done>EndpointEditorView has COLLECTION section with Picker for existing collections, "New Collection" button for creating collections inline, PRO gating, and auto-save on change.</done>
</task>

</tasks>

<verification>
1. Build succeeds with no warnings
2. Endpoint list shows collection filter chips when at least one endpoint has a collectionName
3. Selecting a collection chip filters the list to only endpoints in that collection
4. "All" chip (or deselecting) shows all endpoints
5. Editor's COLLECTION section shows Picker with "None" + existing collection names
6. Creating a new collection from editor assigns the endpoint and shows the new name in filter chips
7. Collection features are PRO-gated (opacity + allowsHitTesting)
8. Duplicating an endpoint preserves its collection assignment
</verification>

<success_criteria>
User can assign endpoints to collections from the editor, filter the endpoint list by collection, and create new collection names. All collection features are PRO-gated. UI matches existing design system (blueprint labels, panel backgrounds, chip styling).
</success_criteria>

<output>
After completion, create `.planning/phases/07-import-export-collections/07-02-SUMMARY.md`
</output>
