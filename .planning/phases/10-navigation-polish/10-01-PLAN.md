---
phase: 10-navigation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MockPad/MockPad/Services/SampleEndpointGenerator.swift
  - MockPad/MockPad/Views/ServerStatusBarView.swift
  - MockPad/MockPad/Views/EmptyStateView.swift
  - MockPad/MockPad/Views/SettingsView.swift
autonomous: true

must_haves:
  truths:
    - "ServerStatusBarView displays server running/stopped state with start/stop toggle"
    - "EmptyStateView shows Create Sample API button when no endpoints exist"
    - "SampleEndpointGenerator creates 4 CRUD endpoints (GET /api/users, GET /api/users/:id, POST /api/users, DELETE /api/users/:id)"
    - "SettingsView includes port config, CORS toggle, auto-start toggle, clear log, import/export, about, and ecosystem links"
  artifacts:
    - path: "MockPad/MockPad/Services/SampleEndpointGenerator.swift"
      provides: "Static sample endpoint factory"
      contains: "enum SampleEndpointGenerator"
    - path: "MockPad/MockPad/Views/ServerStatusBarView.swift"
      provides: "Reusable server status bar"
      contains: "struct ServerStatusBarView"
    - path: "MockPad/MockPad/Views/EmptyStateView.swift"
      provides: "Empty state with quick-start"
      contains: "struct EmptyStateView"
    - path: "MockPad/MockPad/Views/SettingsView.swift"
      provides: "Settings screen"
      contains: "struct SettingsView"
  key_links:
    - from: "MockPad/MockPad/Views/EmptyStateView.swift"
      to: "SampleEndpointGenerator"
      via: "calls createSampleEndpoints()"
      pattern: "SampleEndpointGenerator\\.createSampleEndpoints"
    - from: "MockPad/MockPad/Views/SettingsView.swift"
      to: "ServerStore"
      via: "@Environment for server config"
      pattern: "@Environment\\(ServerStore\\.self\\)"
---

<objective>
Create the new building-block views and services needed for Phase 10 navigation: ServerStatusBarView (reusable server status indicator with start/stop), EmptyStateView (quick-start flow with sample API generation), SampleEndpointGenerator (4 CRUD endpoints factory), and SettingsView (server config, data, about, ecosystem sections).

Purpose: These views are prerequisites for the adaptive ContentView refactor in Plan 10-03. Building them independently keeps Plan 10-03 focused on navigation wiring.
Output: 4 new Swift files ready to be composed into iPad and iPhone navigation layouts.
</objective>

<execution_context>
@/home/claudeuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-navigation-polish/10-RESEARCH.md
@MockPad/MockPad/App/ServerStore.swift
@MockPad/MockPad/App/EndpointStore.swift
@MockPad/MockPad/Theme/MockPadColors.swift
@MockPad/MockPad/Theme/MockPadTypography.swift
@MockPad/MockPad/Theme/MockPadMetrics.swift
@MockPad/MockPad/Models/MockEndpoint.swift
@MockPad/MockPad/Models/ServerConfiguration.swift
@MockPad/MockPad/Views/EndpointListView.swift
@MockPad/MockPad/App/ProManager.swift
@MockPad/MockPad/Services/CollectionExporter.swift
@MockPad/MockPad/Services/CollectionImporter.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SampleEndpointGenerator and ServerStatusBarView</name>
  <files>
    MockPad/MockPad/Services/SampleEndpointGenerator.swift
    MockPad/MockPad/Views/ServerStatusBarView.swift
  </files>
  <action>
Create SampleEndpointGenerator as a caseless enum (matches BuiltInTemplates, CurlGenerator convention) at MockPad/MockPad/Services/SampleEndpointGenerator.swift:

```swift
enum SampleEndpointGenerator {
    static func createSampleEndpoints() -> [MockEndpoint]
}
```

Generate 4 sample CRUD endpoints with realistic JSON response bodies:
1. GET /api/users -- 200, JSON array with 2 user objects (id, name, email), sorted keys, 2-space indent
2. GET /api/users/:id -- 200, single user object with id/name/email
3. POST /api/users -- 201, JSON with id + "User created" message
4. DELETE /api/users/:id -- 204, empty response body ""

Each endpoint gets sequential sortOrder (0-3). Use `responseHeaders: ["Content-Type": "application/json"]` defaults (the MockEndpoint init default handles this).

Create ServerStatusBarView at MockPad/MockPad/Views/ServerStatusBarView.swift:

```swift
struct ServerStatusBarView: View
```

- Reads `@Environment(ServerStore.self)` and `@Environment(EndpointStore.self)`
- Horizontal layout: status dot (green running / red stopped using MockPadColors.serverRunning / .serverStopped), VStack with status text ("SERVER: RUNNING" / "SERVER: STOPPED" in MockPadTypography.serverStatus) and server URL (serverStore.serverURL in MockPadTypography.logTimestamp), Spacer, start/stop Button
- Button label: "STOP" (red-ish) when running, "START" (green-ish) when stopped, using MockPadTypography.badge
- Button action: `Task { if serverStore.isRunning { await serverStore.stopServer() } else { await serverStore.startServer(endpointStore: endpointStore) } }`
- Use MockPadMetrics.panelPadding for horizontal padding, MockPadMetrics.serverStatusBarHeight for frame height
- Background: MockPadColors.panel
- Add a thin bottom border (1px) using MockPadColors.elevated overlay for visual separation

DO NOT add #Preview macros -- these views use @Environment which requires specific setup.
  </action>
  <verify>Both files exist at expected paths. SampleEndpointGenerator has createSampleEndpoints() returning 4 MockEndpoint instances. ServerStatusBarView reads ServerStore and EndpointStore from environment.</verify>
  <done>SampleEndpointGenerator creates 4 CRUD endpoints with proper paths, methods, status codes, and JSON bodies. ServerStatusBarView shows server status with start/stop button.</done>
</task>

<task type="auto">
  <name>Task 2: Create EmptyStateView and SettingsView</name>
  <files>
    MockPad/MockPad/Views/EmptyStateView.swift
    MockPad/MockPad/Views/SettingsView.swift
  </files>
  <action>
Create EmptyStateView at MockPad/MockPad/Views/EmptyStateView.swift:

```swift
struct EmptyStateView: View
```

- Reads `@Environment(EndpointStore.self)`, `@Environment(ServerStore.self)`
- Centered VStack with Spacer above and below:
  - Icon: `Image(systemName: "antenna.radiowaves.left.and.right.slash")` at 48pt in MockPadColors.textDisabled
  - Title: "No Endpoints" in MockPadTypography.sectionTitle, MockPadColors.textSecondary
  - Subtitle: "Create a sample REST API to get started" in MockPadTypography.bodySmall, MockPadColors.textMuted
  - "Create Sample API" Button with MockPadColors.accent foreground, bordered style (.buttonStyle(.bordered))
  - Button action: call `createSampleAPI()` private method
- `createSampleAPI()`:
  1. Get endpoints from `SampleEndpointGenerator.createSampleEndpoints()`
  2. For each endpoint, call `endpointStore.addEndpoint(endpoint)`
  3. After adding all, `Task { await serverStore.startServer(endpointStore: endpointStore) }` to auto-start

Create SettingsView at MockPad/MockPad/Views/SettingsView.swift:

```swift
struct SettingsView: View
```

- Reads `@Environment(ServerStore.self)`, `@Environment(EndpointStore.self)`, `@Environment(ProManager.self)`
- Uses `@State private var portText: String` initialized from `String(serverStore.port)` in .onAppear
- Uses `@State private var showImporter = false`, `@State private var showExporter = false`, `@State private var exportDocument: MockPadDocument?`, `@State private var importError: String?`, `@State private var showImportError = false`, `@State private var pendingImport: MockPadExport?`, `@State private var showImportPreview = false`

Form with sections:
1. **SERVER_ section** (header styled with `.font(MockPadTypography.sectionLabel).foregroundStyle(MockPadColors.accent)`):
   - Port: TextField("Port", text: $portText) with .keyboardType(.numberPad). On .onChange of portText, parse to UInt16, clamp 1024-65535, write to serverStore.port. Only update if valid.
   - CORS toggle: `@Bindable var serverBinding = serverStore` then `Toggle("CORS Headers", isOn: $serverBinding.corsEnabled)` -- or use Binding(get:/set:) wrapper with serverStore.corsEnabled
   - Auto-start toggle: same pattern for serverStore.autoStart

2. **DATA_ section**:
   - "Clear Request Log" Button (destructive role) calling `endpointStore.clearLog()`
   - "Import Endpoints" Button setting `showImporter = true`
   - "Export Endpoints" Button (PRO-gated) creating export document and setting `showExporter = true`

3. **ABOUT_ section**:
   - "Version" row: HStack with Text("Version"), Spacer, Text with app version from `Bundle.main.infoDictionary?["CFBundleShortVersionString"]` and build from `CFBundleVersion`
   - "Build" row if desired, or combine into single "Version X.Y (Build Z)" display

4. **MORE APPS_ section**:
   - Link rows for ProbePad, DeltaPad, GuardPad, BeaconPad -- each with `Link("ProbePad", destination: URL(string: "https://apps.apple.com/app/probepad/id6504769581")!)` (use placeholder App Store URLs for now since exact IDs may vary). Use Image(systemName: "arrow.up.forward.app") as trailing indicator. Style with MockPadColors.textSecondary.
   - Use "itms-apps://apps.apple.com/app/idXXXXXX" URL scheme to open directly in App Store. For now use placeholder URLs.

Form styling:
- `.scrollContentBackground(.hidden)` + `.background(MockPadColors.background)`
- `.navigationTitle("Settings")`
- Section headers use `Text("> SECTION_NAME_")` pattern with accent color

Attach .fileImporter, .fileExporter, .sheet(importPreview), and .alert(importError) modifiers -- reuse the same import/export logic patterns from EndpointListView but simplified (no OpenAPI import, no collection filtering -- exports all endpoints, imports from file).

Note: The `localhostOnly` toggle will be added by Plan 10-02 which runs in parallel. The ContentView refactor (Plan 10-03) will compose SettingsView into the navigation. DO NOT add localhostOnly toggle here -- Plan 10-02 will add it when it creates the property.
  </action>
  <verify>Both files exist at expected paths. EmptyStateView has "Create Sample API" button calling SampleEndpointGenerator. SettingsView has Form with SERVER_, DATA_, ABOUT_, MORE APPS_ sections.</verify>
  <done>EmptyStateView shows empty state with sample API creation button and auto-starts server. SettingsView includes port config, CORS toggle, auto-start toggle, clear log, import/export, version info, and ecosystem links.</done>
</task>

</tasks>

<verification>
- SampleEndpointGenerator.createSampleEndpoints() returns exactly 4 MockEndpoint instances with correct paths, methods, and status codes
- ServerStatusBarView compiles with ServerStore and EndpointStore environment dependencies
- EmptyStateView creates sample endpoints and auto-starts server on button tap
- SettingsView Form has all 4 required sections with correct controls
- All files use MockPadColors, MockPadTypography, MockPadMetrics design tokens consistently
- No #Preview macros that would fail without environment setup
</verification>

<success_criteria>
- 4 new Swift files created in correct locations
- All views use established Slate Blueprint design tokens
- SampleEndpointGenerator follows caseless enum convention
- SettingsView covers all NAVI-04 requirements except localhost-only (Plan 10-02)
- EmptyStateView covers NAVI-03 requirements
- ServerStatusBarView is ready for composition in both iPad and iPhone layouts
</success_criteria>

<output>
After completion, create `.planning/phases/10-navigation-polish/10-01-SUMMARY.md`
</output>
