---
phase: 02-server-engine-core
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - MockPad/MockPad/Services/HTTPRequestParser.swift
  - MockPad/MockPad/Services/HTTPResponseBuilder.swift
  - MockPad/MockPad/Services/EndpointMatcher.swift
  - MockPad/MockPadTests/HTTPRequestParserTests.swift
  - MockPad/MockPadTests/HTTPResponseBuilderTests.swift
  - MockPad/MockPadTests/EndpointMatcherTests.swift
autonomous: true

must_haves:
  truths:
    - "HTTPRequestParser.parse(data:) returns a fully populated ParsedRequest for valid GET/POST/PUT/DELETE requests"
    - "HTTPRequestParser.parse(data:) returns nil for malformed HTTP data"
    - "HTTPRequestParser.parseQueryString extracts key-value pairs from URL query strings"
    - "HTTPResponseBuilder.build produces valid HTTP/1.1 response with correct Content-Length, Connection: close, and Server header"
    - "HTTPResponseBuilder.build includes CORS headers when corsEnabled is true and omits them when false"
    - "HTTPResponseBuilder.buildPreflightResponse returns 204 with CORS headers and max-age"
    - "EndpointMatcher.match returns .matched for exact path+method match on enabled endpoints"
    - "EndpointMatcher.match returns .notFound when no enabled endpoint matches the path"
    - "EndpointMatcher.match returns .methodNotAllowed with allowed methods list when path matches but method does not"
    - "EndpointMatcher.match ignores disabled endpoints"
  artifacts:
    - path: "MockPad/MockPad/Services/HTTPRequestParser.swift"
      provides: "Pure HTTP request parsing from raw Data"
      contains: "nonisolated static func parse"
    - path: "MockPad/MockPad/Services/HTTPResponseBuilder.swift"
      provides: "Pure HTTP response building to Data"
      contains: "nonisolated static func build"
    - path: "MockPad/MockPad/Services/EndpointMatcher.swift"
      provides: "Pure endpoint matching by path and method"
      contains: "nonisolated static func match"
    - path: "MockPad/MockPadTests/HTTPRequestParserTests.swift"
      provides: "Unit tests for HTTP request parsing"
      contains: "@Test func"
    - path: "MockPad/MockPadTests/HTTPResponseBuilderTests.swift"
      provides: "Unit tests for HTTP response building"
      contains: "@Test func"
    - path: "MockPad/MockPadTests/EndpointMatcherTests.swift"
      provides: "Unit tests for endpoint matching"
      contains: "@Test func"
  key_links:
    - from: "MockPad/MockPad/Services/HTTPRequestParser.swift"
      to: "Data -> ParsedRequest?"
      via: "Pure function, no dependencies"
      pattern: "nonisolated static func parse.*Data.*ParsedRequest"
    - from: "MockPad/MockPad/Services/HTTPResponseBuilder.swift"
      to: "statusCode + headers + body -> Data"
      via: "Pure function, no dependencies"
      pattern: "nonisolated static func build.*Int.*Data"
    - from: "MockPad/MockPad/Services/EndpointMatcher.swift"
      to: "method + path + endpoints -> MatchResult"
      via: "Pure function, accepts tuple array (not SwiftData model)"
      pattern: "nonisolated static func match.*MatchResult"
---

<objective>
Implement three pure caseless-enum HTTP services (HTTPRequestParser, HTTPResponseBuilder, EndpointMatcher) with comprehensive TDD coverage.

Purpose: These services form the stateless HTTP protocol layer called by MockServerEngine (Plan 02). Building them first with full test coverage ensures the HTTP parsing, response building, and endpoint routing logic is correct before wiring into the actor.

Output: 3 service files + 3 test files with ~35 unit tests covering all HTTP protocol edge cases.
</objective>

<execution_context>
@/home/claudeuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-server-engine-core/02-RESEARCH.md
@MockPad/MockPad/Models/HTTPMethod.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD HTTPRequestParser service</name>
  <files>MockPad/MockPad/Services/HTTPRequestParser.swift, MockPad/MockPadTests/HTTPRequestParserTests.swift</files>
  <action>
RED: Create HTTPRequestParserTests.swift with these test cases:
- parseSimpleGET: "GET /api/users HTTP/1.1\r\nHost: localhost\r\n\r\n" -> method "GET", path "/api/users", body nil
- parsePOSTWithJSONBody: "POST /api/users HTTP/1.1\r\nContent-Type: application/json\r\n\r\n{\"name\":\"John\"}" -> method "POST", body "{\"name\":\"John\"}"
- parsePUTWithBody: PUT request with body -> method "PUT", correct body
- parseDELETE: DELETE request -> method "DELETE", path correct
- parseQueryString: "GET /api/users?page=1&limit=20 HTTP/1.1\r\nHost: localhost\r\n\r\n" -> path "/api/users", queryString "page=1&limit=20"
- parseMultipleHeaders: request with Host, Content-Type, Authorization -> all 3 headers present in dictionary
- headerWithColonInValue: "Host: localhost:8080" -> value "localhost:8080" (colon in value preserved)
- emptyBody_returnsNil: request with \r\n\r\n but no body content -> body is nil
- invalidData_returnsNil: "not an http request" -> returns nil
- emptyData_returnsNil: empty Data() -> returns nil
- requestLineOnly_parsesMinimal: "GET / HTTP/1.1\r\n\r\n" -> method "GET", path "/"
- parseQueryStringHelper_multipleParams: "page=1&limit=20" -> ["page": "1", "limit": "20"]
- parseQueryStringHelper_encodedValues: "name=John%20Doe" -> ["name": "John Doe"]
- parseQueryStringHelper_emptyString: "" -> [:]
- parseQueryStringHelper_nil: nil -> [:]

Create test file with `import Testing`, `import Foundation`, `@testable import MockPad`. Use struct-based suite, `@Test func`, `#expect()`. Test target does NOT have MainActor default isolation, so no `@MainActor` needed on test struct (pure functions).

Run tests -- all MUST fail (RED).

GREEN: Create HTTPRequestParser.swift as caseless enum with:
- `struct ParsedRequest: Sendable` with fields: method (String), path (String), queryString (String?), httpVersion (String), headers ([String: String]), body (String?)
- `nonisolated static func parse(data: Data) -> ParsedRequest?` -- split at \r\n\r\n for headers/body, parse request line (METHOD /path HTTP/1.1), parse headers (Key: Value with maxSplits: 1 for colons in values), return nil for malformed input, return nil for empty body strings
- `nonisolated static func parseQueryString(_ queryString: String?) -> [String: String]` -- split on &, split on = with maxSplits: 1, apply removingPercentEncoding

Run tests -- all MUST pass (GREEN).

IMPORTANT: All static methods MUST be `nonisolated` to be callable from the MockServerEngine actor without crossing to MainActor (SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor in main target).
  </action>
  <verify>All HTTPRequestParser tests pass. Build succeeds with no warnings.</verify>
  <done>HTTPRequestParser correctly parses GET/POST/PUT/DELETE requests with headers, query strings, and bodies. Returns nil for malformed input. 15 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: TDD HTTPResponseBuilder service</name>
  <files>MockPad/MockPad/Services/HTTPResponseBuilder.swift, MockPad/MockPadTests/HTTPResponseBuilderTests.swift</files>
  <action>
RED: Create HTTPResponseBuilderTests.swift with these test cases:
- build200Response: statusCode 200, body "{}" -> response string contains "HTTP/1.1 200 OK", "Content-Length: 2", body "{}"
- buildWithCustomHeaders: pass ["X-Custom": "value"] -> response contains "X-Custom: value"
- buildWithCORSEnabled: corsEnabled true -> response contains "Access-Control-Allow-Origin: *", "Access-Control-Allow-Methods:", "Access-Control-Allow-Headers:"
- buildWithCORSDisabled: corsEnabled false -> response does NOT contain "Access-Control-Allow-Origin"
- buildConnectionClose: any response -> contains "Connection: close"
- buildServerHeader: any response -> contains "Server: MockPad/1.0"
- buildContentLength: body "hello" -> "Content-Length: 5"
- build404Response: statusCode 404 -> "HTTP/1.1 404 Not Found"
- build405Response: statusCode 405 -> "HTTP/1.1 405 Method Not Allowed"
- build400Response: statusCode 400 -> "HTTP/1.1 400 Bad Request"
- buildPreflightResponse: -> "HTTP/1.1 204 No Content", "Access-Control-Max-Age: 86400", all CORS headers present
- buildPreflightNoBody: preflight response body portion is empty
- buildEmptyBody: body "" -> Content-Length: 0

Parse response Data back to String in tests for assertion. Use `import Testing`, `import Foundation`, `@testable import MockPad`.

Run tests -- all MUST fail (RED).

GREEN: Create HTTPResponseBuilder.swift as caseless enum with:
- `nonisolated static func build(statusCode: Int, headers: [String: String] = [:], body: String = "", corsEnabled: Bool = true) -> Data` -- build "HTTP/1.1 {code} {phrase}\r\n" + headers (sorted for deterministic output) + "\r\n" + body. Always include Server: MockPad/1.0, Content-Length, Connection: close, Date (HTTP date format). Add CORS headers when corsEnabled=true (allow user headers to override defaults).
- `nonisolated static func buildPreflightResponse(corsEnabled: Bool) -> Data` -- calls build with statusCode 204, CORS headers including Access-Control-Max-Age: 86400, empty body
- `nonisolated private static func statusPhrase(_ code: Int) -> String` -- switch for 200/201/204/400/401/403/404/405/429/500, default "Unknown"
- `nonisolated private static func httpDateString() -> String` -- DateFormatter with en_US_POSIX locale, GMT timezone, RFC 7231 format

Run tests -- all MUST pass (GREEN).
  </action>
  <verify>All HTTPResponseBuilder tests pass. Build succeeds with no warnings.</verify>
  <done>HTTPResponseBuilder produces valid HTTP/1.1 responses with correct status lines, headers, CORS support, and Connection: close. Preflight returns 204 with max-age. 13 tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: TDD EndpointMatcher service</name>
  <files>MockPad/MockPad/Services/EndpointMatcher.swift, MockPad/MockPadTests/EndpointMatcherTests.swift</files>
  <action>
RED: Create EndpointMatcherTests.swift with these test cases:
- exactMatch_returnsMatched: GET /api/users with matching endpoint -> .matched with correct statusCode, responseBody, responseHeaders
- methodNotAllowed_returnsAllowedMethods: POST /api/users but only GET endpoint exists -> .methodNotAllowed with allowedMethods ["GET"]
- noMatch_returnsNotFound: GET /nonexistent with no matching endpoint -> .notFound
- disabledEndpoint_ignored: matching endpoint with isEnabled=false -> .notFound
- caseInsensitivePath: GET /API/Users matches endpoint with path /api/users -> .matched
- caseInsensitiveMethod: get /api/users matches endpoint with method GET -> .matched
- multipleMethodsOnSamePath: GET and POST on /api/users, request DELETE -> .methodNotAllowed with allowedMethods containing both "GET" and "POST"
- emptyEndpoints_returnsNotFound: empty endpoint list -> .notFound
- firstMatchWins: two GET /api/users endpoints, first has status 200, second has 201 -> returns 200 (first match)

Use `EndpointMatcher.EndpointData` typealias for the tuple to keep tests clean. Use `import Testing`, `import Foundation`, `@testable import MockPad`.

Run tests -- all MUST fail (RED).

GREEN: Create EndpointMatcher.swift as caseless enum with:
- `enum MatchResult: Sendable` with cases: `.matched(path: String, method: String, statusCode: Int, responseBody: String, responseHeaders: [String: String])`, `.notFound`, `.methodNotAllowed(allowedMethods: [String])`
- `typealias EndpointData = (path: String, method: String, statusCode: Int, responseBody: String, responseHeaders: [String: String], isEnabled: Bool)`
- `nonisolated static func match(method: String, path: String, endpoints: [EndpointData]) -> MatchResult` -- filter enabled endpoints, find path matches (case-insensitive), find method match (case-insensitive), return .matched/.methodNotAllowed/.notFound

Phase 2 implements EXACT path matching only. Path parameters (:id) and wildcards (*) are Phase 6.

Run tests -- all MUST pass (GREEN).
  </action>
  <verify>All EndpointMatcher tests pass. Build succeeds with no warnings.</verify>
  <done>EndpointMatcher correctly routes requests to endpoints by exact path+method match, returns 404 for unmatched paths, returns 405 with allowed methods for unmatched methods, and ignores disabled endpoints. 9 tests pass.</done>
</task>

</tasks>

<verification>
1. All ~37 tests pass across 3 test files
2. Build succeeds with zero warnings
3. All static methods are marked `nonisolated` (verify with grep)
4. All service enums are caseless (no cases, no init)
5. ParsedRequest and MatchResult conform to Sendable
</verification>

<success_criteria>
- HTTPRequestParser parses valid HTTP/1.0 and HTTP/1.1 requests into structured ParsedRequest
- HTTPRequestParser returns nil for malformed input
- HTTPResponseBuilder produces valid HTTP response bytes with correct headers
- HTTPResponseBuilder supports CORS toggle and preflight responses
- EndpointMatcher correctly handles matched, not-found, and method-not-allowed scenarios
- All services are nonisolated static functions callable from any actor
- ~37 unit tests all pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-server-engine-core/02-01-SUMMARY.md`
</output>
