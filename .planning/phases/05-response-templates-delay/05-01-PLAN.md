---
phase: 05-response-templates-delay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MockPad/MockPad/Models/MockEndpoint.swift
  - MockPad/MockPad/Models/ResponseTemplate.swift
  - MockPad/MockPad/Services/BuiltInTemplates.swift
  - MockPad/MockPad/Services/EndpointSnapshot.swift
  - MockPad/MockPad/App/EndpointStore.swift
  - MockPad/MockPad/MockPadApp.swift
autonomous: true

must_haves:
  truths:
    - "MockEndpoint has responseDelayMs field with default 0 (lightweight migration safe)"
    - "EndpointSnapshot carries responseDelayMs across actor boundary"
    - "ResponseTemplate @Model persists custom templates in SwiftData"
    - "8 built-in templates exist as static data with correct names, status codes, bodies, and headers"
    - "ModelContainer registers ResponseTemplate.self alongside existing models"
  artifacts:
    - path: "MockPad/MockPad/Models/MockEndpoint.swift"
      provides: "responseDelayMs Int field with default 0"
      contains: "responseDelayMs"
    - path: "MockPad/MockPad/Models/ResponseTemplate.swift"
      provides: "SwiftData @Model for custom response templates"
      contains: "@Model"
    - path: "MockPad/MockPad/Services/BuiltInTemplates.swift"
      provides: "8 static template definitions"
      contains: "static let all"
    - path: "MockPad/MockPad/Services/EndpointSnapshot.swift"
      provides: "responseDelayMs field on Sendable DTO"
      contains: "responseDelayMs"
    - path: "MockPad/MockPad/MockPadApp.swift"
      provides: "ResponseTemplate registered in ModelContainer"
      contains: "ResponseTemplate.self"
  key_links:
    - from: "MockPad/MockPad/App/EndpointStore.swift"
      to: "MockPad/MockPad/Services/EndpointSnapshot.swift"
      via: "endpointSnapshots maps responseDelayMs"
      pattern: "responseDelayMs.*endpoint\\.responseDelayMs"
---

<objective>
Add the data layer for response templates and delay simulation. This creates the SwiftData model for custom templates, adds the delay field to the endpoint data pipeline, and defines the 8 built-in response templates as static data.

Purpose: Establish the data foundation that the template picker UI (Plan 05-02) and server engine delay (Plan 05-03) depend on.
Output: ResponseTemplate model, BuiltInTemplates enum, MockEndpoint.responseDelayMs field, EndpointSnapshot.responseDelayMs field, ModelContainer registration.
</objective>

<execution_context>
@/home/claudeuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-response-templates-delay/05-RESEARCH.md

@MockPad/MockPad/Models/MockEndpoint.swift
@MockPad/MockPad/Services/EndpointSnapshot.swift
@MockPad/MockPad/App/EndpointStore.swift
@MockPad/MockPad/MockPadApp.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responseDelayMs to data pipeline and ResponseTemplate model</name>
  <files>
    MockPad/MockPad/Models/MockEndpoint.swift
    MockPad/MockPad/Models/ResponseTemplate.swift
    MockPad/MockPad/Services/EndpointSnapshot.swift
    MockPad/MockPad/App/EndpointStore.swift
    MockPad/MockPad/MockPadApp.swift
  </files>
  <action>
    **MockEndpoint.swift** -- Add `var responseDelayMs: Int` stored property (default 0 for lightweight migration). Add `responseDelayMs: Int = 0` parameter to init and assign `self.responseDelayMs = responseDelayMs`. The default 0 ensures existing persisted MockEndpoint records load correctly without migration.

    **EndpointSnapshot.swift** -- Add `let responseDelayMs: Int` field to the struct. Place it after `isEnabled`.

    **EndpointStore.swift** -- In the `endpointSnapshots` computed property, add `responseDelayMs: endpoint.responseDelayMs` to the EndpointSnapshot initializer call.

    **ResponseTemplate.swift** -- Create NEW file. Define `@Model final class ResponseTemplate` with fields:
    - `var name: String`
    - `var statusCode: Int`
    - `var responseBody: String`
    - `var responseHeadersData: Data?` (JSON-encoded [String: String], same pattern as MockEndpoint)
    - `var createdAt: Date`
    - Computed `var responseHeaders: [String: String]` (get/set with JSONDecoder/JSONEncoder, exact same pattern as MockEndpoint.responseHeaders)
    - `init(name:statusCode:responseBody:responseHeaders:)` that sets createdAt = Date() and encodes headers

    **MockPadApp.swift** -- Add `ResponseTemplate.self` to the `ModelContainer(for:)` call: `ModelContainer(for: MockEndpoint.self, RequestLog.self, ResponseTemplate.self)`.
  </action>
  <verify>Project builds. Check that ResponseTemplate.self is in ModelContainer. Check MockEndpoint has responseDelayMs. Check EndpointSnapshot has responseDelayMs.</verify>
  <done>MockEndpoint persists responseDelayMs with default 0. EndpointSnapshot carries responseDelayMs. ResponseTemplate model is registered in ModelContainer. EndpointStore maps delay through snapshot pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Create BuiltInTemplates with 8 template definitions</name>
  <files>
    MockPad/MockPad/Services/BuiltInTemplates.swift
  </files>
  <action>
    Create NEW file `MockPad/MockPad/Services/BuiltInTemplates.swift`.

    Define caseless enum `BuiltInTemplates` (matches HTTPMethod, EndpointMatcher pattern of caseless enum as namespace).

    Inner `struct Template: Sendable, Identifiable` with fields:
    - `let id: String` (unique slug for identity)
    - `let name: String` (display name)
    - `let statusCode: Int`
    - `let responseBody: String` (pretty-printed JSON with .sortedKeys convention)
    - `let responseHeaders: [String: String]`
    - `let icon: String` (SF Symbol name)

    Define `static let all: [Template]` containing exactly these 8 templates in order:

    1. **Success** -- id: "success", statusCode: 200, body: `{"message":"OK","success":true}` (pretty), icon: "checkmark.circle"
    2. **User Object** -- id: "user-object", statusCode: 200, body: user JSON with id/name/email/role, icon: "person.circle"
    3. **User List** -- id: "user-list", statusCode: 200, body: `{"data":[...2 users...],"total":2}`, icon: "person.2.circle"
    4. **Not Found** -- id: "not-found", statusCode: 404, body: `{"error":"Not Found","message":"The requested resource was not found"}`, icon: "questionmark.circle"
    5. **Unauthorized** -- id: "unauthorized", statusCode: 401, body: `{"error":"Unauthorized","message":"Authentication required"}`, headers include "WWW-Authenticate": "Bearer", icon: "lock.circle"
    6. **Validation Error** -- id: "validation-error", statusCode: 422, body with errors array (email + name fields), icon: "exclamationmark.triangle"
    7. **Server Error** -- id: "server-error", statusCode: 500, body: `{"error":"Internal Server Error","message":"An unexpected error occurred"}`, icon: "xmark.octagon"
    8. **Rate Limited** -- id: "rate-limited", statusCode: 429, body with retryAfter: 60, headers include "Retry-After": "60", icon: "gauge.with.needle"

    All templates include `"Content-Type": "application/json"` in responseHeaders. JSON bodies use sorted keys and 2-space indentation (matching existing JSONSerialization convention from ResponseBodyEditorView).

    Also define each template as a named static property (e.g., `static let success = Template(...)`) for direct access, with `all` composed from them.
  </action>
  <verify>Project builds. BuiltInTemplates.all.count == 8. Each template has non-empty name, valid statusCode, non-empty responseBody, non-empty responseHeaders, non-empty icon.</verify>
  <done>8 built-in templates exist as static data: Success, User Object, User List, Not Found, Unauthorized, Validation Error, Server Error, Rate Limited. Each template carries name, statusCode, responseBody, responseHeaders, and SF Symbol icon.</done>
</task>

</tasks>

<verification>
- MockEndpoint.responseDelayMs exists with default 0
- EndpointSnapshot.responseDelayMs exists
- EndpointStore.endpointSnapshots maps responseDelayMs
- ResponseTemplate @Model is defined with name, statusCode, responseBody, responseHeadersData, createdAt
- ResponseTemplate.self is registered in ModelContainer
- BuiltInTemplates.all contains exactly 8 templates
- All 8 template names match: Success, User Object, User List, Not Found, Unauthorized, Validation Error, Server Error, Rate Limited
- Project builds cleanly
</verification>

<success_criteria>
- Data pipeline carries responseDelayMs from MockEndpoint -> EndpointSnapshot -> (ready for engine in Plan 05-03)
- ResponseTemplate model is persisted via SwiftData
- 8 built-in templates are available as static data for the template picker (Plan 05-02)
</success_criteria>

<output>
After completion, create `.planning/phases/05-response-templates-delay/05-01-SUMMARY.md`
</output>
