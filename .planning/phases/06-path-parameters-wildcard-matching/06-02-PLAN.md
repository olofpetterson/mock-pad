---
phase: 06-path-parameters-wildcard-matching
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - MockPad/MockPad/Services/MockServerEngine.swift
autonomous: true

must_haves:
  truths:
    - "Server extracts path parameter values from matched endpoints and substitutes {token} in response body"
    - "Request to /api/users/42 against endpoint /api/users/:id returns response with {id} replaced by 42"
    - "Wildcard endpoint /api/* catches unmatched sub-paths and returns configured response"
    - "404/405 error responses are NOT processed by PathParamReplacer (no endpoint-configured body)"
  artifacts:
    - path: "MockPad/MockPad/Services/MockServerEngine.swift"
      provides: "pathParams extraction from MatchResult + PathParamReplacer.replace call before response build"
      contains: "PathParamReplacer.replace"
  key_links:
    - from: "MockServerEngine.handleReceivedData"
      to: "PathParamReplacer.replace(in:with:)"
      via: "applies token substitution to response body in .matched case"
      pattern: "PathParamReplacer\\.replace"
    - from: "MockServerEngine.handleReceivedData"
      to: "MatchResult.matched pathParams"
      via: "destructures 7th associated value from match result"
      pattern: "let pathParams"
---

<objective>
Integrate path parameter extraction and token substitution into MockServerEngine's request processing pipeline.

Purpose: Wire the enhanced EndpointMatcher (from Plan 06-01) into the live server so incoming requests to parameterized/wildcard endpoints receive response bodies with {token} substitution applied. Completes ENDP-07 and RESP-09 end-to-end.
Output: Updated MockServerEngine with PathParamReplacer integration.
</objective>

<execution_context>
@/home/claudeuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-path-parameters-wildcard-matching/06-01-SUMMARY.md
@MockPad/MockPad/Services/MockServerEngine.swift
@MockPad/MockPad/Services/EndpointMatcher.swift
@MockPad/MockPad/Services/PathParamReplacer.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate PathParamReplacer into MockServerEngine response pipeline</name>
  <files>MockPad/MockPad/Services/MockServerEngine.swift</files>
  <action>
Update the `.matched` case destructuring in `handleReceivedData` to extract the new `pathParams` (7th associated value) from MatchResult.

Current destructuring (line ~274):
```swift
case .matched(let path, _, let statusCode, let body, let headers, let matchedDelayMs):
```

Update to:
```swift
case .matched(let path, _, let statusCode, let body, let headers, let matchedDelayMs, let pathParams):
```

After extracting pathParams, apply PathParamReplacer BEFORE building the HTTP response. Insert after `matchedEndpointPath = path` and before the `var responseHeaders = headers` line:

```swift
let resolvedBody = PathParamReplacer.replace(in: body, with: pathParams)
```

Then use `resolvedBody` instead of `body` in the three places the response body is used:
1. `responseBody = resolvedBody` (for the request log)
2. `HTTPResponseBuilder.build(statusCode:headers:body:corsEnabled:)` -- pass `resolvedBody` as body parameter

Do NOT modify the .notFound or .methodNotAllowed cases -- they have server-generated error bodies with no path parameter tokens.

The change is ~4 lines: updated destructuring pattern, one `let resolvedBody` line, and replacing `body` references with `resolvedBody`.
  </action>
  <verify>
1. Build succeeds: `swift build` passes with no errors
2. Verify PathParamReplacer.replace is called in MockServerEngine (grep for "PathParamReplacer.replace")
3. Verify destructuring includes pathParams (grep for "let pathParams" in MockServerEngine)
4. Verify resolvedBody is used for both log and response build (grep for "resolvedBody")
5. Run `swift test` -- all tests still pass (engine has no direct tests but matcher tests exercise the data flow)
  </verify>
  <done>MockServerEngine extracts pathParams from matched endpoints and applies PathParamReplacer.replace(in:with:) to the response body before building the HTTP response. Token substitution is live for all matched endpoints. Error responses (404, 405) are unaffected.</done>
</task>

</tasks>

<verification>
1. `swift build` compiles without errors
2. `swift test` all tests pass
3. MockServerEngine.swift contains `PathParamReplacer.replace(in:` call in the .matched case
4. MockServerEngine.swift destructures `pathParams` from MatchResult.matched
5. The `resolvedBody` variable is used for both responseBody assignment and HTTPResponseBuilder.build call
6. .notFound and .methodNotAllowed cases are unchanged
</verification>

<success_criteria>
- MockServerEngine applies token substitution to matched endpoint response bodies
- Path parameter tokens ({id}, {userId}) in response body are replaced with actual values from request path
- Error responses (404, 405) are not processed by PathParamReplacer
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-path-parameters-wildcard-matching/06-02-SUMMARY.md`
</output>
